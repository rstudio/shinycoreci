name: Run Bedrock remediation
description: Use Claude Code Action with Anthropic models on AWS Bedrock to create a remediation pull request.

inputs:
  github-token:
    description: GitHub App installation token for the target repository. Needs actions:read, contents:write, issues:write, and pull-requests:write.
    required: true
  aws-role-to-assume:
    description: AWS IAM role ARN that can invoke Anthropic models through Bedrock.
    required: true
  aws-region:
    description: AWS region for Bedrock runtime calls.
    required: true
  bedrock-model-id:
    description: Bedrock Anthropic model ID or inference profile ID used by Claude Code.
    required: true
  bedrock-fallback-model-id:
    description: Optional Bedrock Anthropic model ID to use as Claude Code's fallback model.
    required: false
    default: ""
  target-owner:
    description: Owner of the repository to fix.
    required: true
  target-repo:
    description: Repository to fix.
    required: true
  target-workflow:
    description: Workflow file that failed.
    required: true
  target-ref:
    description: Branch or ref to fix.
    required: true
  target-maintainer:
    description: Maintainer who should review the generated PR.
    required: true
  app-slug:
    description: >
      Slug of the GitHub App that issued github-token (e.g. "posit-shiny-automation").
      Used to disambiguate workflow_dispatch runs created during verification.
    required: false
    default: ""
  issue-url:
    description: Remediation issue URL to link from the generated PR.
    required: true
  remote-run-id:
    description: GitHub Actions run ID for the failed remote workflow.
    required: false
    default: ""
  remote-run-url:
    description: URL of the failed remote workflow run.
    required: false
    default: ""
  remote-run-conclusion:
    description: Final conclusion reported by the failed remote workflow run.
    required: true
  source-repository:
    description: Repository running the scheduler workflow.
    required: true
  source-run-url:
    description: URL of the scheduler workflow run.
    required: true
  max-turns:
    description: Maximum Claude Code agent turns.
    required: false
    default: "20"
  max-attempts:
    description: >
      Maximum number of remediation attempts (initial attempt + retries) Claude
      will make. Each attempt dispatches the target workflow on the PR branch,
      watches the run, and on failure re-invokes Claude with the new logs to
      push additional commits onto the same PR branch. Capped at 2.
    required: false
    default: "2"
  verify-watch-attempts:
    description: How many times to poll for the verification run to complete.
    required: false
    default: "90"
  verify-watch-interval-seconds:
    description: Seconds between verification watch attempts.
    required: false
    default: "60"
  verify-lookup-attempts:
    description: How many times to poll for the verification run to appear.
    required: false
    default: "30"
  verify-lookup-interval-seconds:
    description: Seconds between verification lookup attempts.
    required: false
    default: "10"

outputs:
  pr_url:
    description: URL of the generated remediation PR, when one was created.
    value: ${{ steps.solve.outputs.pr_url }}
  pr_number:
    description: Number of the generated remediation PR, when one was created.
    value: ${{ steps.solve.outputs.pr_number }}
  verification_conclusion:
    description: >
      Final conclusion of the dispatched target workflow run on the PR branch
      after the last remediation attempt. Empty if no PR was opened.
    value: ${{ steps.verify-2.outputs.conclusion || steps.verify-1.outputs.conclusion }}
  attempts_used:
    description: Number of remediation attempts actually executed (1 or 2).
    value: ${{ steps.verify-2.outputs.attempt || steps.verify-1.outputs.attempt || '0' }}

runs:
  using: composite
  steps:
    - name: Prepare verifier script
      id: verifier
      shell: bash
      run: |
        set -euo pipefail
        path="${RUNNER_TEMP}/shinycoreci-bedrock-verify.sh"
        cp "${GITHUB_ACTION_PATH}/verify.sh" "${path}"
        chmod +x "${path}"
        echo "path=${path}" >> "$GITHUB_OUTPUT"

    - name: Checkout target repository
      uses: actions/checkout@v6
      with:
        repository: ${{ inputs.target-owner }}/${{ inputs.target-repo }}
        ref: ${{ inputs.target-ref }}
        token: ${{ inputs.github-token }}

    - name: Sanitize AWS role session name
      id: session-name
      shell: bash
      env:
        TARGET_OWNER: ${{ inputs.target-owner }}
        TARGET_REPO: ${{ inputs.target-repo }}
      run: |
        set -euo pipefail
        raw="shinycoreci-${TARGET_OWNER}-${TARGET_REPO}-${GITHUB_RUN_ID:-manual}"
        value="$(sed 's/[^a-zA-Z0-9_+=,.@-]/_/g' <<<"${raw}" | cut -c1-64)"
        echo "value=${value}" >> "$GITHUB_OUTPUT"

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v6
      with:
        role-to-assume: ${{ inputs.aws-role-to-assume }}
        role-session-name: ${{ steps.session-name.outputs.value }}
        aws-region: ${{ inputs.aws-region }}

    - name: Download remote workflow log
      shell: bash
      env:
        TARGET_GH_TOKEN: ${{ inputs.github-token }}
        TARGET_OWNER: ${{ inputs.target-owner }}
        TARGET_REPO: ${{ inputs.target-repo }}
        REMOTE_RUN_ID: ${{ inputs.remote-run-id }}
        REMOTE_RUN_URL: ${{ inputs.remote-run-url }}
      run: |
        set -euo pipefail

        mkdir -p .shinycoreci-remediation
        echo ".shinycoreci-remediation/" >> .git/info/exclude

        log_file=".shinycoreci-remediation/remote-run.log"
        if [[ -n "${REMOTE_RUN_ID}" ]]; then
          if ! GH_TOKEN="${TARGET_GH_TOKEN}" gh run view "${REMOTE_RUN_ID}" --repo "${TARGET_OWNER}/${TARGET_REPO}" --log > "${log_file}"; then
            {
              echo "Could not download logs for remote run ${REMOTE_RUN_ID}."
              echo "Remote run URL: ${REMOTE_RUN_URL:-not available}"
            } > "${log_file}"
          fi
        else
          {
            echo "No remote run ID was available."
            echo "Remote run URL: ${REMOTE_RUN_URL:-not available}"
          } > "${log_file}"
        fi

    - name: Run Claude Code Action
      id: claude
      uses: anthropics/claude-code-action@v1.0.92
      with:
        use_bedrock: "true"
        github_token: ${{ inputs.github-token }}
        base_branch: ${{ inputs.target-ref }}
        branch_prefix: shinycoreci-bedrock-
        additional_permissions: "actions: read"
        allowed_bots: ${{ inputs.app-slug != '' && format('github-actions[bot],posit-shiny-automation[bot],{0}[bot]', inputs.app-slug) || 'github-actions[bot],posit-shiny-automation[bot]' }}
        prompt: |
          You are fixing a failed cross-repository package check for ${{ inputs.target-owner }}/${{ inputs.target-repo }}.

          Context:
          - Target branch/ref: ${{ inputs.target-ref }}
          - Failed workflow: ${{ inputs.target-workflow }}
          - Remote conclusion: ${{ inputs.remote-run-conclusion }}
          - Remote run: ${{ inputs.remote-run-url || 'not available' }}
          - Scheduler repository: ${{ inputs.source-repository }}
          - Scheduler run: ${{ inputs.source-run-url }}
          - Remediation issue: ${{ inputs.issue-url }}

          The remote workflow log is available at .shinycoreci-remediation/remote-run.log. Investigate the failure, modify only files needed to fix it, and run focused verification when possible. Prefer the existing project conventions.

          Do not add a repository-specific reviewer workflow; ${{ inputs.source-repository }} handles reviewer and assignee handoff centrally. If scheduler compatibility requires a workflow change in this repository, limit it to ${{ inputs.target-workflow }}.

          Any commit message you write must follow Conventional Commits (https://www.conventionalcommits.org/en/v1.0.0/) in the form `<type>(optional scope): <description>`. Prefer `fix(ci): ...` for package-check fixes; the wrapping workflow uses the same format for the final PR title.

          Leave the final git commit, branch push, pull request creation, reviewer assignment, and issue comment to this workflow. Do not commit, push, create pull requests, or modify .shinycoreci-remediation.
        claude_args: |
          --model ${{ inputs.bedrock-model-id }}
          ${{ inputs.bedrock-fallback-model-id != '' && format('--fallback-model {0}', inputs.bedrock-fallback-model-id) || '' }}
          --max-turns ${{ inputs.max-turns }}
          --allowedTools Bash,Read,Write,Edit,WebFetch,WebSearch

    - name: Finalize remediation pull request
      id: solve
      shell: bash
      env:
        TARGET_GH_TOKEN: ${{ inputs.github-token }}
        TARGET_OWNER: ${{ inputs.target-owner }}
        TARGET_REPO: ${{ inputs.target-repo }}
        TARGET_WORKFLOW: ${{ inputs.target-workflow }}
        TARGET_REF: ${{ inputs.target-ref }}
        TARGET_MAINTAINER: ${{ inputs.target-maintainer }}
        ISSUE_URL: ${{ inputs.issue-url }}
        REMOTE_RUN_URL: ${{ inputs.remote-run-url }}
        REMOTE_RUN_CONCLUSION: ${{ inputs.remote-run-conclusion }}
        SOURCE_REPOSITORY: ${{ inputs.source-repository }}
        SOURCE_RUN_URL: ${{ inputs.source-run-url }}
        CLAUDE_BRANCH_NAME: ${{ steps.claude.outputs.branch_name }}
      run: |
        set -euo pipefail

        git remote set-url origin "https://x-access-token:${TARGET_GH_TOKEN}@github.com/${TARGET_OWNER}/${TARGET_REPO}.git"
        git config user.name "shinycoreci-bedrock[bot]"
        git config user.email "shinycoreci-bedrock[bot]@users.noreply.github.com"
        git fetch origin "${TARGET_REF}:refs/remotes/origin/${TARGET_REF}" --depth=1

        rm -rf .shinycoreci-remediation

        status="$(git status --porcelain --untracked-files=all)"
        current_branch="$(git branch --show-current || true)"
        branch="${CLAUDE_BRANCH_NAME:-${current_branch}}"

        if [[ -z "${branch}" || "${branch}" == "HEAD" || "${branch}" == "${TARGET_REF}" ]]; then
          safe_workflow="$(tr -c '[:alnum:]_.-' '-' <<<"${TARGET_WORKFLOW}" | sed -E 's/-+/-/g; s/^-//; s/-$//')"
          branch="shinycoreci/bedrock-remediation-${GITHUB_RUN_ID:-manual}-${GITHUB_RUN_ATTEMPT:-1}-${safe_workflow}"
          git switch -c "${branch}"
        elif [[ "${current_branch}" != "${branch}" ]]; then
          if git show-ref --verify --quiet "refs/heads/${branch}"; then
            git switch "${branch}"
          else
            git switch -c "${branch}"
          fi
        fi

        if [[ -n "${status}" ]]; then
          git add -A
          diff_stat="$(git diff --cached --stat)"
          pr_title="fix(ci): remediate ${TARGET_REPO} package checks on ${TARGET_REF}"
          commit_body="$(printf '%s\n' \
            "Automated by shinycoreci using Claude Code Action with Anthropic models on AWS Bedrock." \
            "" \
            "Refs: ${ISSUE_URL}" \
            "Remote workflow: ${TARGET_WORKFLOW}" \
            "Remote conclusion: ${REMOTE_RUN_CONCLUSION}" \
            "Remote run: ${REMOTE_RUN_URL:-not available}" \
            "Scheduler run: ${SOURCE_RUN_URL}")"
          git commit -m "${pr_title}" -m "${commit_body}"
        else
          diff_stat="$(git diff --stat "origin/${TARGET_REF}...HEAD" || true)"
        fi

        ahead_count="$(git rev-list --count "origin/${TARGET_REF}..HEAD" 2>/dev/null || echo 0)"
        if [[ "${ahead_count}" == "0" ]]; then
          issue_number="${ISSUE_URL##*/}"
          GH_TOKEN="${TARGET_GH_TOKEN}" gh issue comment "${issue_number}" \
            --repo "${TARGET_OWNER}/${TARGET_REPO}" \
            --body "AWS Bedrock remediation ran through Claude Code Action, but no file changes were available to propose." || true
          echo "pr_url=" >> "$GITHUB_OUTPUT"
          echo "pr_number=" >> "$GITHUB_OUTPUT"
          exit 0
        fi

        git push --set-upstream origin "${branch}"

        issue_number="${ISSUE_URL##*/}"
        pr_url="$(GH_TOKEN="${TARGET_GH_TOKEN}" gh pr list \
          --repo "${TARGET_OWNER}/${TARGET_REPO}" \
          --head "${branch}" \
          --state open \
          --json url \
          --jq '.[0].url // empty')"

        if [[ -z "${pr_url}" ]]; then
          pr_title="fix(ci): remediate ${TARGET_REPO} package checks on ${TARGET_REF}"
          body_file="$(mktemp)"
          {
            echo "## Summary"
            echo ""
            echo "Automated remediation for the failed package checks dispatched by \`${SOURCE_REPOSITORY}\`."
            echo ""
            echo "## Failure context"
            echo ""
            echo "| Field | Value |"
            echo "| --- | --- |"
            echo "| Target ref | \`${TARGET_REF}\` |"
            echo "| Remote workflow | \`${TARGET_WORKFLOW}\` |"
            echo "| Remote conclusion | \`${REMOTE_RUN_CONCLUSION}\` |"
            echo "| Remote run | ${REMOTE_RUN_URL:-not available} |"
            echo "| Scheduler run | ${SOURCE_RUN_URL} |"
            echo "| Solver | Claude Code Action using Anthropic models on AWS Bedrock |"
            echo ""
            echo "## Diff summary"
            echo ""
            echo "\`\`\`"
            echo "${diff_stat}"
            echo "\`\`\`"
            echo ""
            echo "## Reviewer checklist"
            echo ""
            echo "- [ ] Title follows [Conventional Commits](https://www.conventionalcommits.org/en/v1.0.0/) (\`<type>(scope): <description>\`)."
            echo "- [ ] Changes are scoped to fixing the failure described above."
            echo "- [ ] No repository-specific reviewer workflow was added; reviewer routing stays in \`${SOURCE_REPOSITORY}\`."
            echo ""
            echo "Closes #${issue_number}"
          } > "${body_file}"

          pr_url="$(GH_TOKEN="${TARGET_GH_TOKEN}" gh pr create \
            --repo "${TARGET_OWNER}/${TARGET_REPO}" \
            --base "${TARGET_REF}" \
            --head "${branch}" \
            --title "${pr_title}" \
            --body-file "${body_file}" \
            --reviewer "${TARGET_MAINTAINER}" \
            --assignee "${TARGET_MAINTAINER}")"
        else
          pr_number="${pr_url##*/}"
          GH_TOKEN="${TARGET_GH_TOKEN}" gh api \
            --method POST \
            -H "Accept: application/vnd.github+json" \
            "/repos/${TARGET_OWNER}/${TARGET_REPO}/pulls/${pr_number}/requested_reviewers" \
            -f "reviewers[]=${TARGET_MAINTAINER}" >/dev/null 2>&1 || true
          GH_TOKEN="${TARGET_GH_TOKEN}" gh api \
            --method POST \
            -H "Accept: application/vnd.github+json" \
            "/repos/${TARGET_OWNER}/${TARGET_REPO}/issues/${pr_number}/assignees" \
            -f "assignees[]=${TARGET_MAINTAINER}" >/dev/null 2>&1 || true
        fi

        GH_TOKEN="${TARGET_GH_TOKEN}" gh issue comment "${issue_number}" \
          --repo "${TARGET_OWNER}/${TARGET_REPO}" \
          --body "Opened AWS Bedrock remediation PR: ${pr_url}" || true

        echo "pr_url=${pr_url}" >> "$GITHUB_OUTPUT"
        echo "pr_number=${pr_url##*/}" >> "$GITHUB_OUTPUT"
        echo "branch=${branch}" >> "$GITHUB_OUTPUT"
        echo "Opened remediation PR: ${pr_url}"

    - name: Verify remediation (attempt 1)
      id: verify-1
      if: steps.solve.outputs.pr_url != ''
      shell: bash
      env:
        TARGET_GH_TOKEN: ${{ inputs.github-token }}
        TARGET_OWNER: ${{ inputs.target-owner }}
        TARGET_REPO: ${{ inputs.target-repo }}
        TARGET_WORKFLOW: ${{ inputs.target-workflow }}
        PR_BRANCH: ${{ steps.solve.outputs.branch }}
        PR_URL: ${{ steps.solve.outputs.pr_url }}
        ISSUE_URL: ${{ inputs.issue-url }}
        APP_SLUG: ${{ inputs.app-slug }}
        LOOKUP_ATTEMPTS: ${{ inputs.verify-lookup-attempts }}
        LOOKUP_INTERVAL: ${{ inputs.verify-lookup-interval-seconds }}
        WATCH_ATTEMPTS: ${{ inputs.verify-watch-attempts }}
        WATCH_INTERVAL: ${{ inputs.verify-watch-interval-seconds }}
      run: |
        set -euo pipefail
        bash "${{ steps.verifier.outputs.path }}" 1

    - name: Run Claude Code Action (retry attempt 2)
      id: claude-retry
      if: >-
        steps.verify-1.outputs.conclusion != ''
        && steps.verify-1.outputs.conclusion != 'success'
        && fromJSON(inputs.max-attempts) >= 2
      uses: anthropics/claude-code-action@v1.0.92
      with:
        use_bedrock: "true"
        github_token: ${{ inputs.github-token }}
        base_branch: ${{ steps.solve.outputs.branch }}
        branch_prefix: shinycoreci-bedrock-retry-
        additional_permissions: "actions: read"
        allowed_bots: ${{ inputs.app-slug != '' && format('github-actions[bot],posit-shiny-automation[bot],shinycoreci-bedrock[bot],{0}[bot]', inputs.app-slug) || 'github-actions[bot],posit-shiny-automation[bot],shinycoreci-bedrock[bot]' }}
        prompt: |
          You are continuing a remediation attempt for ${{ inputs.target-owner }}/${{ inputs.target-repo }}.
          The first remediation attempt opened PR ${{ steps.solve.outputs.pr_url }} on branch `${{ steps.solve.outputs.branch }}`,
          but the dispatched verification run still failed with conclusion `${{ steps.verify-1.outputs.conclusion }}`.

          Context:
          - PR branch (already contains your previous fix): ${{ steps.solve.outputs.branch }}
          - Failed verification run: ${{ steps.verify-1.outputs.run_url }}
          - Original failed workflow: ${{ inputs.target-workflow }}
          - Remediation issue: ${{ inputs.issue-url }}

          The latest verification log is available at .shinycoreci-remediation/remote-run.log. Review it together with your prior changes (already on disk via the PR branch) and add additional fixes that resolve the remaining failures. Do not revert your earlier work unless it was clearly wrong.

          Same constraints as before: do not add a repository-specific reviewer workflow; restrict any workflow change to ${{ inputs.target-workflow }}. Any commit message must follow Conventional Commits (`fix(ci): ...` for package-check fixes).

          Leave the final git commit, branch push, and PR update to this workflow. Do not commit, push, or modify .shinycoreci-remediation.
        claude_args: |
          --model ${{ inputs.bedrock-model-id }}
          ${{ inputs.bedrock-fallback-model-id != '' && format('--fallback-model {0}', inputs.bedrock-fallback-model-id) || '' }}
          --max-turns ${{ inputs.max-turns }}
          --allowedTools Bash,Read,Write,Edit,WebFetch,WebSearch

    - name: Finalize retry commit on PR branch
      id: solve-retry
      if: steps.claude-retry.outcome == 'success'
      shell: bash
      env:
        TARGET_GH_TOKEN: ${{ inputs.github-token }}
        TARGET_OWNER: ${{ inputs.target-owner }}
        TARGET_REPO: ${{ inputs.target-repo }}
        TARGET_REF: ${{ inputs.target-ref }}
        TARGET_WORKFLOW: ${{ inputs.target-workflow }}
        ISSUE_URL: ${{ inputs.issue-url }}
        REMOTE_RUN_URL: ${{ steps.verify-1.outputs.run_url }}
        REMOTE_RUN_CONCLUSION: ${{ steps.verify-1.outputs.conclusion }}
        SOURCE_RUN_URL: ${{ inputs.source-run-url }}
        PR_BRANCH: ${{ steps.solve.outputs.branch }}
        PR_URL: ${{ steps.solve.outputs.pr_url }}
        CLAUDE_BRANCH_NAME: ${{ steps.claude-retry.outputs.branch_name }}
      run: |
        set -euo pipefail

        git remote set-url origin "https://x-access-token:${TARGET_GH_TOKEN}@github.com/${TARGET_OWNER}/${TARGET_REPO}.git"
        git config user.name "shinycoreci-bedrock[bot]"
        git config user.email "shinycoreci-bedrock[bot]@users.noreply.github.com"

        rm -rf .shinycoreci-remediation || true

        pr_title="fix(ci): follow-up remediation for ${TARGET_REPO} package checks"
        commit_body="$(printf '%s\n' \
          "Second AWS Bedrock remediation attempt addressing remaining failures." \
          "" \
          "Refs: ${ISSUE_URL}" \
          "PR: ${PR_URL}" \
          "Previous verification conclusion: ${REMOTE_RUN_CONCLUSION}" \
          "Previous verification run: ${REMOTE_RUN_URL:-not available}" \
          "Scheduler run: ${SOURCE_RUN_URL}")"

        retry_branch="${CLAUDE_BRANCH_NAME:-}"
        current_branch="$(git branch --show-current || true)"
        [[ -z "${retry_branch}" ]] && retry_branch="${current_branch}"

        git add -A
        if ! git diff --cached --quiet; then
          git commit -m "${pr_title}" -m "${commit_body}"
        fi

        retry_ref="$(git rev-parse HEAD)"
        git fetch origin "${PR_BRANCH}:refs/remotes/origin/${PR_BRANCH}" --depth=50

        if [[ -z "${retry_branch}" || "${retry_branch}" == "${PR_BRANCH}" || "${current_branch}" == "${PR_BRANCH}" ]]; then
          ahead_count="$(git rev-list --count "origin/${PR_BRANCH}..HEAD" 2>/dev/null || echo 0)"
          if [[ "${ahead_count}" != "0" ]]; then
            git push origin "HEAD:${PR_BRANCH}"
            echo "pushed=true" >> "$GITHUB_OUTPUT"
          else
            echo "pushed=false" >> "$GITHUB_OUTPUT"
          fi
          exit 0
        fi

        # Squash retry branch into the PR branch with a single conventional commit.
        git fetch origin "${retry_branch}:refs/remotes/origin/${retry_branch}" --depth=50 || true
        git switch "${PR_BRANCH}" 2>/dev/null || git switch -c "${PR_BRANCH}" "origin/${PR_BRANCH}"
        git reset --hard "origin/${PR_BRANCH}"

        if ! git merge --squash "${retry_ref}" 2>/dev/null && \
           ! git merge --squash "${retry_branch}" 2>/dev/null && \
           ! git merge --squash "origin/${retry_branch}" 2>/dev/null; then
          echo "::warning::Retry branch ${retry_branch} could not be squashed onto ${PR_BRANCH}; skipping."
          echo "pushed=false" >> "$GITHUB_OUTPUT"
          exit 0
        fi

        if git diff --cached --quiet; then
          echo "pushed=false" >> "$GITHUB_OUTPUT"
          exit 0
        fi

        git commit -m "${pr_title}" -m "${commit_body}"
        git push origin "HEAD:${PR_BRANCH}"

        # Try to delete the throwaway retry branch (best effort).
        GH_TOKEN="${TARGET_GH_TOKEN}" gh api \
          --method DELETE \
          -H "Accept: application/vnd.github+json" \
          "/repos/${TARGET_OWNER}/${TARGET_REPO}/git/refs/heads/${retry_branch}" >/dev/null 2>&1 || true

        echo "pushed=true" >> "$GITHUB_OUTPUT"

    - name: Verify remediation (attempt 2)
      id: verify-2
      if: steps.solve-retry.outputs.pushed == 'true'
      shell: bash
      env:
        TARGET_GH_TOKEN: ${{ inputs.github-token }}
        TARGET_OWNER: ${{ inputs.target-owner }}
        TARGET_REPO: ${{ inputs.target-repo }}
        TARGET_WORKFLOW: ${{ inputs.target-workflow }}
        PR_BRANCH: ${{ steps.solve.outputs.branch }}
        PR_URL: ${{ steps.solve.outputs.pr_url }}
        ISSUE_URL: ${{ inputs.issue-url }}
        APP_SLUG: ${{ inputs.app-slug }}
        LOOKUP_ATTEMPTS: ${{ inputs.verify-lookup-attempts }}
        LOOKUP_INTERVAL: ${{ inputs.verify-lookup-interval-seconds }}
        WATCH_ATTEMPTS: ${{ inputs.verify-watch-attempts }}
        WATCH_INTERVAL: ${{ inputs.verify-watch-interval-seconds }}
      run: |
        set -euo pipefail
        bash "${{ steps.verifier.outputs.path }}" 2

    - name: Comment final remediation status
      if: always() && steps.solve.outputs.pr_url != ''
      shell: bash
      env:
        TARGET_GH_TOKEN: ${{ inputs.github-token }}
        TARGET_OWNER: ${{ inputs.target-owner }}
        TARGET_REPO: ${{ inputs.target-repo }}
        ISSUE_URL: ${{ inputs.issue-url }}
        PR_URL: ${{ steps.solve.outputs.pr_url }}
        ATTEMPT_1_CONCLUSION: ${{ steps.verify-1.outputs.conclusion }}
        ATTEMPT_1_RUN_URL: ${{ steps.verify-1.outputs.run_url }}
        ATTEMPT_2_CONCLUSION: ${{ steps.verify-2.outputs.conclusion }}
        ATTEMPT_2_RUN_URL: ${{ steps.verify-2.outputs.run_url }}
      run: |
        set -euo pipefail

        [[ -z "${ATTEMPT_1_CONCLUSION}" ]] && exit 0

        final_conclusion="${ATTEMPT_2_CONCLUSION:-${ATTEMPT_1_CONCLUSION}}"
        attempts_used=1
        [[ -n "${ATTEMPT_2_CONCLUSION}" ]] && attempts_used=2

        if [[ "${final_conclusion}" == "success" ]]; then
          status_line="Verification succeeded after ${attempts_used} attempt(s)."
        else
          status_line="Verification still failing after ${attempts_used} attempt(s) (final conclusion: \`${final_conclusion}\`). Manual review required."
        fi

        if [[ -n "${ATTEMPT_2_CONCLUSION}" ]]; then
          attempt_2_line="- Attempt 2 conclusion: \`${ATTEMPT_2_CONCLUSION}\` (${ATTEMPT_2_RUN_URL:-no run url})"
        else
          attempt_2_line="- Attempt 2: not run"
        fi

        body="$(printf '%s\n' \
          "${status_line}" \
          "" \
          "- PR: ${PR_URL}" \
          "- Attempt 1 conclusion: \`${ATTEMPT_1_CONCLUSION}\` (${ATTEMPT_1_RUN_URL:-no run url})" \
          "${attempt_2_line}")"

        issue_number="${ISSUE_URL##*/}"
        GH_TOKEN="${TARGET_GH_TOKEN}" gh issue comment "${issue_number}" \
          --repo "${TARGET_OWNER}/${TARGET_REPO}" \
          --body "${body}" || true

        pr_number="${PR_URL##*/}"
        GH_TOKEN="${TARGET_GH_TOKEN}" gh pr comment "${pr_number}" \
          --repo "${TARGET_OWNER}/${TARGET_REPO}" \
          --body "${body}" || true
