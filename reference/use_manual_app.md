# Flag an app to be manually tested

All `apps_*` methods inspect each application to determine if if testing
is possible.

## Usage

``` r
use_manual_app(app_dir)
```

## Arguments

- app_dir:

  Shiny application directory containing an app.R, ui.R, server.R, or
  index.Rmd
