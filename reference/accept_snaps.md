# Accept all snapshot changes

Accepts **all** snapshot changes for every shiny application in
`inst/apps`.

## Usage

``` r
accept_snaps(repo_dir = rprojroot::find_package_root_file())
```

## Arguments

- repo_dir:

  Root repo folder path
